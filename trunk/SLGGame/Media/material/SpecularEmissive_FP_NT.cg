void main(
	out float4 oColour	: COLOR,
	
	uniform float3 sspecular,
	uniform float3 semissive,
	uniform float3 sdiffuse 
	)
{
	oColour = float4(sdiffuse.x, sspecular.x , semissive.x , 1.0);
}