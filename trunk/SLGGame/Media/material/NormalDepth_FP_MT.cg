void main(
	float3	normal	:	TEXCOORD0,
	float3	viewpos	:	TEXCOORD1,
	
	out float4 oColour	: COLOR,
	
	uniform	float	farChip
	)
{
	float normdepth = -viewpos.z / farChip;
	float3 worldnormal = normal * 0.5 + 0.5;
	oColour = float4(worldnormal, normdepth);
}